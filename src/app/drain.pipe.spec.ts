/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DrainPipe } from './drain.pipe';

describe('DrainPipe', () => {
  it('create an instance', () => {
    const pipe = new DrainPipe();
    expect(pipe).toBeTruthy();
  });
});
